#include <iostream>
#include <math.h>
using namespace std;



int pow(b,p);

int main()
{

    int p,b, res;
    cout<<"Enter base";
    cin>>b;
    cout<<"Enter pow";
    cin>>p;

    res= pow(b,p);

    return 0;
}
    int pow(b,p)
     {
         for (p=1; res<=p; p++)
            cout=b*b;
     }
